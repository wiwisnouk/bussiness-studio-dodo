name: Deploy to Render

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run tests (if any)
        run: |
          echo "Add your tests here if needed"
      
      - name: Deploy to Render
        env:
          RENDER_API_KEY: ${{rnd_TNEmfVr3kpMGMOWjy0dGKak2xyt3}}
        run: |
          echo "Deployment will be handled by Render webhook or manual trigger"
          echo "Make sure to set up Render service with GitHub integration"

